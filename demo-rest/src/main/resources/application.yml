spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/#{DBName}
    username: #{Id}
    password: #{Password}
    driver-class-name: org.mariadb.jdbc.Driver
  data:
    rest:
      base-path: /api
      default-page-size: 10
      max-page-size: 10
server:
  port: 8081